# Func Linux -- Natverk & Sakerhetsverktyg (Kali-inspirerat)
# OBS: Verktyg markerade med [PIP] eller [MANUELL] installeras via scripts/install.sh

# Scanning & Enumeration
nmap
masscan
netdiscover
# enum4linux -- ej i Debian repos, installeras manuellt

# Paketanalys
wireshark
tshark
tcpdump
ettercap-text-only

# Web
nikto
gobuster
ffuf
dirb
sqlmap
whatweb
# wpscan -- ej i Debian repos, installeras via gem

# Exploitation (metasploit installeras via eget skript)
# exploitdb -- ej i Debian repos, installeras manuellt

# Losenord
john
hashcat
hydra
medusa
crunch
# wordlists -- ej i Debian repos (Kali meta-paket), installeras manuellt

# Wireless
aircrack-ng
reaver
bully
# kismet -- ej i Debian bookworm, installeras manuellt
wifite

# Natverk
netcat-openbsd
socat
ncat
proxychains4
sshuttle
sshpass
openvpn
wireguard-tools

# HTTP-verktyg
curl
wget
httpie

# DNS
dnsenum
dnsrecon
fierce

# MITM / Sniffing
arpwatch
dsniff
mitmproxy
bettercap
netsniff-ng
# responder -- ej i Debian repos, installeras manuellt

# Reverse engineering
# radare2 -- ej i Debian bookworm, installeras manuellt
binwalk
foremost
# ghidra -- installeras manuellt (ej i Debian repos)

# Forensik
autopsy
sleuthkit
dc3dd
testdisk
scalpel
libimage-exiftool-perl

# Pivoting & Tunneling
stunnel4
# chisel -- installeras manuellt (ej i Debian repos)

# Post-Exploitation
python3-impacket
# neo4j -- kraver extern repo, installeras manuellt
# crackmapexec, bloodhound, evil-winrm -- installeras via pip/manuellt

# Fuzzing
afl++

# Container & Cloud
awscli

# VPN & Anonymitet
tor
torsocks
i2pd

# Vulnerability scanning
# openvas (GVM) -- kraver extern repo, installeras manuellt
